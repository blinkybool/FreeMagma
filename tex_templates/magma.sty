\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{magma}[2020/01/24 Magma]

\RequirePackage{tikz, forest}

\usepackage{forest}
\forestset{%
  cbt/.style={
    for tree={
      circle,
      draw,
      fill,
      s sep'+=10pt,
      fit=band,
      if n children=0{fill=none}{},
    },
    % root node style
    where level=0{rectangle, fill=none}{}
  },
}

\newcommand{\something}[5][2]{world}

\newcommand{\triangulation}[3][2]{
  \newcommand{\n}{#2}
  \newcommand{\rad}{#1}
  \newcommand{\labelOffset}{0.4}
  \newcommand{\vertexRadius}{3pt}
  \newcommand{\start}{90+360/\n/2}
  \newcommand{\vertexAngle}[1]{{\start+360/\n*(##1-1)}}
  
  % Draw vertices
  \foreach \i in {1,...,\n} {
  \draw[fill] (\vertexAngle{\i}:\rad) circle (\vertexRadius) node at (\vertexAngle{\i}:\rad+\labelOffset) {\i};
  }

  % Draw boundary
  \draw 
    (\vertexAngle{1}:\rad)
    \foreach \i in {2,...,\n} {-- (\vertexAngle{\i}:\rad)}
    -- cycle;

  % Draw chords
  \foreach \src/\tar in {#3} {
    \draw[red] (\vertexAngle{\src}:\rad) -- (\vertexAngle{\tar}:\rad);
  }
}

\endinput